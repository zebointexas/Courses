
/* Class that contains common char string manipulation utility
 * functions
 */

#include "c_cpp_header.h"
#include "c_cpp_includes.h"

class my_string_class {

public: 

	int		 len;

	void reverse_string(mystring, mystring, int);
	void get_rid_of_padding_blanks(mystring, mystring, int);
	void get_rid_of_padding_blanks_2(mystring, mystring, int);
	void remove_trailing_space_chars (mystring);
	int is_a_blank_string(mystring);
	int is_digit(mystring);
	void to_lower(mystring);
	void to_upper(mystring);

	void substr(mystring, mystring, char);

private:



};
	
